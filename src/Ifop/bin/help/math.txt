+	+ (n1 n2 -- summ)\nПомещает в стек сумму двух верхних операндов.\nКроме того может склеивать строку со строкой и строку с числом.
-	- (n1 n2 -- sub)\nПомещает в стек разность двух верхних операндов (n1 - n2).
*	* (n1 n2 -- summ)\nПомещает в стек произведение двух верхних операндов.
/	/ (n1 n2 -- sub)\nПомещает в стек частное от деления двух верхних операндов\nЕсли n1 целое, то деление будет целочисленным даже если n2 вещественное (дробная часть у n2 будет отброшена).
mod	/mod (n1 n2 -- sub)\nПомещает в стек остаток от деления двух верхних операндов.
>=	>= (n1 n2 -- res)\n
<=	<= (n1 n2 -- res)\n
>	> (n1 n2 -- res)\n
<	< (n1 n2 -- res)\n
=	= (n1 n2 -- res)\n
shl	shl (n1 -- n1')\nСдвигает все биты числа из вершины стека влево.
shr	shr (n1 -- n1')\nСдвигает все биты числа из вершины стека вправо.
negate	negate (n1 -- -n1)\nМеняет знак числа в вершине стека на противоположный.
abs	abs (n1 -- n1')\nПриводит числов из вершины стека к абсолютному значению.

=
>
<
>=
<=

and
or
not
xor

anglemode	AngleMode ( -- s1)\nКладет в вершину стека строку "deg" "rad" или "grad" в зависимости от того в каком режиме считаются углы.\nПо умолчанию стоит режим "Deg" - градусы.
degmode	DegMode ( -- )\nУстанавливает режим измерения угла в градусы.
radmode	RadMode ( -- )\nУстанавливает режим измерения угла в радианы.
gradmode	GradMode ( -- )\nУстанавливает режим измерения угла в грады.